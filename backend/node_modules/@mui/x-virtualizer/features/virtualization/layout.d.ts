import * as React from 'react';
import { Store } from '@mui/x-internals/store';
import type { ColumnWithWidth } from "../../models/index.js";
import { Dimensions } from "../dimensions.js";
import { Virtualization, type VirtualizationLayoutParams } from "./virtualization.js";
import type { BaseState, ParamsWithDefaults } from "../../useVirtualizer.js";
type RequiredAPI = Dimensions.API;
type BaseElements = {
  scroller: React.RefObject<HTMLElement | null>;
  container: React.RefObject<HTMLElement | null>;
};
type AnyElements = BaseElements & Record<string, React.RefObject<HTMLElement | null>>;
export declare abstract class Layout<E extends AnyElements = AnyElements> {
  static elements: readonly (keyof AnyElements)[];
  refs: E;
  constructor(refs: E);
  abstract use(store: Store<BaseState>, params: ParamsWithDefaults, api: RequiredAPI, layoutParams: VirtualizationLayoutParams): any;
  refSetter(name: keyof E): (node: HTMLDivElement | null) => void;
}
type DataGridElements = BaseElements & {
  scrollbarVertical: React.RefObject<HTMLElement | null>;
  scrollbarHorizontal: React.RefObject<HTMLElement | null>;
};
export declare class LayoutDataGrid extends Layout<DataGridElements> {
  static elements: readonly ['scroller', 'container', 'content', 'positioner', 'scrollbarVertical', 'scrollbarHorizontal'];
  use(store: Store<BaseState>, _params: ParamsWithDefaults, api: RequiredAPI, layoutParams: VirtualizationLayoutParams): void;
  static selectors: {
    containerProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      ref: any;
    };
    scrollerProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      ref: any;
      style: {
        overflowX: "hidden" | undefined;
        overflowY: "hidden" | undefined;
      };
      role: string;
      tabIndex: number | undefined;
    };
    scrollerContentProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      style: React.CSSProperties | undefined;
      role: string;
    };
    viewportProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      style: {
        width: number;
        height: number;
      };
      role: string;
    };
    contentProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      style: React.CSSProperties;
      role: string;
    };
    positionerProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      style: {
        transform: string;
      };
    };
    containerVerticalProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      style: {
        transform: string;
      };
    } | undefined;
    scrollbarHorizontalProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      ref: any;
      scrollPosition: {
        current: import("../../index.js").ScrollPosition;
      };
    };
    scrollbarVerticalProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      ref: any;
      scrollPosition: {
        current: import("../../index.js").ScrollPosition;
      };
    };
    scrollAreaProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      scrollPosition: {
        current: import("../../index.js").ScrollPosition;
      };
    };
  };
}
type ListElements = BaseElements;
export declare class LayoutList extends Layout<ListElements> {
  static elements: readonly ['scroller', 'container', 'content', 'positioner'];
  use(store: Store<BaseState>, _params: ParamsWithDefaults, _api: RequiredAPI, layoutParams: VirtualizationLayoutParams): void;
  static selectors: {
    containerProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      ref: any;
      style: React.CSSProperties;
      role: string;
      tabIndex: number | undefined;
    };
    contentProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      style: React.CSSProperties;
      role: string;
    };
    positionerProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      style: React.CSSProperties;
    };
  };
}
/**
 * List layout based on the "inverse sticky" technique
 * (https://pierre.computer/writing/on-rendering-diffs).
 *
 * The rendered window is placed in the normal flow at `offsetTop` (through the positioner
 * spacer) and given `position: sticky` with `top` & `bottom` set to
 * `-(renderedHeight - viewportHeight)`. Within that range the browser moves the window
 * natively with the scroll (no JS needed per frame). When the scroll position moves past
 * the rendered window before the render context updates, the window clamps to the viewport
 * edge instead of scrolling out of view, so stale content is shown instead of a blank area.
 * The rows render inside a `windowContent` box that keeps them paint-stable across
 * render context updates (see the paint-stable window content comment above).
 *
 * Expected DOM structure:
 * ```tsx
 * <div {...containerProps}>         // scroller
 *   <div {...contentProps}>         // in-flow, full content height, sticky containing block
 *     <div {...positionerProps} />  // spacer, height: offsetTop
 *     <div {...windowProps}>        // sticky rendered window
 *       <div {...windowContentProps}>  // anchored local space for the rows
 *         {rows}
 *       </div>
 *     </div>
 *   </div>
 * </div>
 * ```
 */
export declare class LayoutListSticky extends Layout<ListElements> {
  static elements: readonly ['scroller', 'container', 'content', 'positioner', 'window', 'windowContent'];
  use(store: Store<BaseState>, _params: ParamsWithDefaults, _api: RequiredAPI, layoutParams: VirtualizationLayoutParams): void;
  static selectors: {
    containerProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      ref: any;
      style: React.CSSProperties;
      role: string;
      tabIndex: number | undefined;
    };
    contentProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      style: React.CSSProperties;
      role: string;
    };
    positionerProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      style: React.CSSProperties;
    };
    windowProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      style: React.CSSProperties;
    };
    windowContentProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      style: React.CSSProperties;
      role: string;
    };
  };
}
export declare class LayoutGridSticky extends Layout<DataGridElements> {
  static elements: readonly ['scroller', 'container', 'content', 'topContainer', 'spacerLeft', 'innerContainer', 'spacerTop', 'window', 'innerWindow', 'windowContent', 'spacerBottom', 'bottomContainer', 'scrollbarVertical', 'scrollbarHorizontal'];
  use(store: Store<BaseState>, _params: ParamsWithDefaults, api: RequiredAPI, layoutParams: VirtualizationLayoutParams): void;
  static selectors: {
    containerProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      ref: any;
      style: React.CSSProperties;
      role: string;
    };
    scrollerProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      ref: any;
      style: React.CSSProperties;
      role: string;
      tabIndex: number | undefined;
    };
    contentProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      style: React.CSSProperties;
      role: string;
    };
    topContainerProps: () => {
      style: React.CSSProperties;
      role: string;
    };
    bottomContainerProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      style: React.CSSProperties;
      role: string;
    };
    scrollbarVerticalProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      ref: any;
    };
    scrollbarHorizontalProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      ref: any;
    };
    spacerLeftProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State, _columns: ColumnWithWidth[]) => {
      style: React.CSSProperties;
      role: string;
    };
    innerContainerProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State, _columns: ColumnWithWidth[]) => {
      style: React.CSSProperties;
      role: string;
    };
    spacerTopProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      style: React.CSSProperties;
      role: string;
    };
    spacerBottomProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      style: React.CSSProperties;
      role: string;
    };
    windowProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      style: React.CSSProperties;
      role: string;
    };
    innerWindowProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State, _columns: ColumnWithWidth[]) => {
      style: React.CSSProperties;
      role: string;
    };
    windowContentProps: (args_0: Virtualization.State<Layout<AnyElements>> & Dimensions.State) => {
      style: React.CSSProperties;
      role: string;
    };
  };
}
export {};